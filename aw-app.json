{
  "manifest_version": 1,
  "id": "mini-browser",
  "name": "Mini Browser",
  "version": "0.8.0",
  "description": "In-app web browser with a real rewriting proxy \u2014 a vendored Ultraviolet (AGPL-3.0) intercepted client-side by a service worker, backed by this app's own TompHTTP Bare Server v3 (written from the open spec, no Node/Wisp/new process). Ships a dev panel (Requests/Console) next to the Go button. Also pilots the shared aw-app-browser container over CDP (navigate/click/type/eval/screenshot), ported from aw-app-devctl: a mini-browser-browser MCP tool wrapper is contributed for agents via mcp.json, with an HTTP twin under /browser/* for fetching screenshot bytes back onto the workspace filesystem.",
  "tier": "inprocess",
  "requires_ui_refresh": true,
  "publisher": "TekFlox",
  "resource_estimate": {
    "cpu": "low",
    "memory": "low",
    "disk": "low"
  },
  "runtime": {
    "python": ">=3.11",
    "entrypoint": "mini_browser_app.plugin:MiniBrowserAppPlugin",
    "pip_requires": [
      "httpx>=0.24",
      "websockets>=11",
      "mcp>=1.2,<2.0",
      "aiofiles>=23"
    ]
  },
  "permissions": [
    "routes:register",
    "net:outbound"
  ],
  "contributes": {
    "routes": [
      {
        "prefix": "/api/apps/mini-browser"
      }
    ],
    "mcp": {
      "reload_on_save": true
    },
    "skills": [
      {
        "id": "aw-mini-browser",
        "path": "skills/aw-mini-browser/SKILL.md",
        "description": "What Mini Browser is and how its MCP tools actually work \u2014 it pilots the separate shared aw-app-browser (noVNC) container over CDP, not its own iframe. Use whenever asked to pilot a browser for quick assisted navigation/debugging, or when a mini-browser MCP call errors with \"aw-app-browser not reachable over CDP\"."
      }
    ],
    "windows": [
      {
        "id": "mini-browser.main",
        "title": "Mini Browser",
        "body": {
          "type": "declarative",
          "spec": "windows/main.json"
        }
      }
    ]
  },
  "config_schema": {
    "type": "object",
    "properties": {
      "home_url": {
        "type": "string",
        "default": "https://example.com",
        "description": "URL the browser loads on open and on the home button."
      }
    },
    "required": []
  },
  "dependencies": {
    "apps": [
      {
        "id": "devctl",
        "version": ">=0.10.0",
        "required": false,
        "reason": "GET /view/screenshot and the browser_screenshot_view MCP tool delegate to devctl's POST /render/screenshot (a side-container-free Playwright renderer). Without devctl installed, everything else in Mini Browser still works \u2014 only that one screenshot path 502s."
      }
    ]
  },
  "migrations": {}
}
